const userName = "Lukas"
let temperatuur = 20;
     console.log(userName);
     alert(`de temperatuur is ${temperatuur} graden`);
     temperatuur += 5;
      console.log (temperatuur);


// opdr interatie
const askName = prompt(`wat is je naam`);
alert (askName);

const Cookies = confirm(`accept je de cookies?`);
console.log(Cookies);

const myCity = 'Den Haag';
alert('Ik woon in ' + myCity);